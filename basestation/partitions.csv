# Name,   Type, SubType, Offset,  Size, Flags
# Custom single-factory-app table (no OTA needed for v1): matches
# partitions_singleapp.csv's layout/offsets exactly, just with a bigger
# factory partition to comfortably fit the embedded web UI assets (Leaflet +
# our HTML/JS/CSS, ~180KB) on top of the WiFi/HTTP server/ESP-NOW code.
# (The stock "large" single-app Kconfig choice, partitions_singleapp_large.csv,
# was tried first but its bootloader overflows its fixed IRAM region on this
# toolchain for reasons unrelated to app partition size -- switching to an
# explicit custom table at the same 0x8000 offset avoids whatever in that
# specific Kconfig choice triggers it, while still getting the extra room.)
nvs,      data, nvs,     ,        0x6000,
phy_init, data, phy,     ,        0x1000,
factory,  app,  factory, ,        1920K,
